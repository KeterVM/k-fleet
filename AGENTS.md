# K Fleet Repository Guide

## Purpose

K Fleet is a source repository for eight portable Codex workflow skills. Keep it
small, readable as Markdown, workflow-based, and independent of any language or
framework.

## Repository map

- `README.md` documents routing, installation, usage, and design principles.
- `skills/<name>/SKILL.md` is the installable source for one skill.
- `examples/fleet-ledger/` installs and forward-tests the skills on a runnable
  fixture; `.agents/skills/` and `skills-lock.json` are generated by `bunx skills`.
- `scripts/validate-skills.mjs` performs dependency-free repository validation.
- `.github/`, `CONTRIBUTING.md`, and `SECURITY.md` define the public contribution,
  automation, and vulnerability-reporting workflows.
- `LICENSE` and `NOTICE` contain the Apache-2.0 terms and attribution.

## Conventions

- Keep exactly the eight core skills documented in the README unless real usage
  justifies a deliberate scope change.
- Prefix every skill directory and matching frontmatter name with `kf-`.
- Split skills by user intent, not by technology layer.
- Use only supported `name` and `description` frontmatter unless a verified need
  requires optional metadata.
- Make descriptions discriminating enough for implicit routing.
- Keep detailed project facts in the target project's guidance, not in K Fleet.
- Do not add dependencies, build tooling, generated files, or scripts without a
  concrete deterministic benefit.

## Validation

For every change, verify frontmatter, directory/name agreement, routing boundaries,
broken links, placeholders, and README accuracy. When the installed Codex skill
validator is available, run it against each skill directory.

Run the repository checks with:

```sh
node scripts/validate-skills.mjs
cd examples/fleet-ledger && npm test
```
